# Journal

## 2026-08-10 — Journey created

Created `system-dsa` skillup (status Planning). Goal: reduce everyday systems-programming friction via DSA + systems design practice (~6 h/wk, five 2-month chunks, ~10 months). Foundations agreed (cost model; memory-first; indirection vs contiguity; concurrency as protocol; layered design; correctness under constraints). No Project Lab — general systems algorithms only. Implementation default: C++. Resources: seed shortlist; learner adds over time. Cadence: brush-up then from-scratch per chunk. Next: learner reviews `learning.md`, then `approve-plan`, then `learn-session` (Chunk 1 step 1).

## 2026-08-11 — Modules 1–4 woven (grill)

Grilled and locked topic weave into five chunks (status still Planning). Spine: keep 5 chunks, weave domain modules. Module 2 full list; recursive-descent production-grade; Shunting-Yard high-level. Module 3 full list with depth defaults (hash deep; trie practical; hash fns 1–2). Module 4: intrusive DLLs + thin scheduler/timer; locking in Chunk 4; payoff Chunk 5. Module 1: bits/rings/fixed-width implement; MMIO high-level; atomic flags light until Chunk 4. Lexing in (front door to descent); ICG thin. Keep systems spine; thin worklist. Add-on-the-go rules recorded in Goal. Language: C11 primary; C++ OK Chunk 4–5. Resources: per-session adopt, no bulk weak URLs. Next: review `learning.md`, then `approve-plan`.

## 2026-08-11 — Curriculum approved

`approve-plan`: curriculum validated (Goal, Foundations, woven Modules 1–4 Curriculum, Milestones, Mastery, Mental Models, References). No Project Lab. Status → Approved. Planning done — lighter model OK for sessions/quizzes. Next: `learn-session` (Chunk 1 step 1 — cost model refresh).
