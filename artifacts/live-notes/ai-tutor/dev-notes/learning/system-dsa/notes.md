# Notes

Complementary scratch (not load-bearing for handoff). Fold decisions into `learning.md`.

## Defaults (agent)

- Implementation language: **C11 primary**; C++ allowed in Chunk 4–5 for threads/atomics (record language per session).
- Cadence intent: ~6 hours/week; skillup sessions ~20 min; from-scratch may span multiple sessions within a chunk.
- Chunk calendar: five sequential 2-month chunks; start date TBD after `approve-plan`.
- Depth: many topics tunable on the go; recursive-descent stays protected if a chunk runs hot.

## Add on the go (locked)

- Small add → append curriculum step + journal; stay Active.
- Cluster add → park here → `review-progress` or explicit add ask → update `# Curriculum`.
- Foundations/goal shift → `replan-learning`.

## Open questions

- Preferred C toolchain (clang/gcc, sanitizers) for from-scratch?
- Where to keep from-scratch code (scratch dir in this repo vs separate practice repo)?

## Parking lot

- Bind a Project Lab later (LLVM / OS tree) only if goals shift — may need `replan-learning` if foundations change.
- Stronger per-topic resources when sessions hit: Crafting Interpreters; FreeRTOS architecture; Embedded Artistry bit ops; etc.
- Optional later expands: Patricia tries; full Unicode lexer; SSA/LLVM IR; priority inheritance; SMP scheduler.
